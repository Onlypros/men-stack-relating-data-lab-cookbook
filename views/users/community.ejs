<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explore Our Community</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Welcome to the Community <%= user.username %></h1>
    <ul>
        <% users.forEach(user => { %> 
          <li>
            <!-- Link to the user's pantry page -->
            <a href="/users/<%= user._id %>/pantry">
              User: <%= user.username %>
            </a>
          </li>
        <% }); %>
      </ul>
</body>
</html>